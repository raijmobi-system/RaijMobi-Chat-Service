[project]
name = "chat-service"
version = "0.1.0"
dependencies = [
    "django>=5.2,<5.3",
    "djangorestframework>=3.15",
    "channels>=4.0",
    "channels-kafka>=0.9",   # se continuar com Kafka
    "kafka-python>=2.0",
    "uvicorn[standard]>=0.30",
    "httpx>=0.27",           # para chamadas REST entre serviços
]
